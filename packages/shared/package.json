{
  "name": "shared",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -b",
    "generate:index": "node scripts/generate-index.cjs",
    "watch:tsc": "tsc -b --watch",
    "watch:index": "chokidar \"src/**/*.ts\" -i \"src/index.ts\" -c \"pnpm generate:index\" --initial --sequential"
  },
  "devDependencies": {
    "@types/node": "^22.18.13",
    "chokidar-cli": "^3.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.6.3"
  }
}
